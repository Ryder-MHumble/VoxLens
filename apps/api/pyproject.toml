[project]
name = "voxlens-backend"
version = "0.1.0"
description = "VoxLens FastAPI backend for multimodal social-video DeepResearch."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.7.0",
  "python-dotenv>=1.0.1",
  "yt-dlp>=2025.1.0",
]
